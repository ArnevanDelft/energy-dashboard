# Base dependencies (this layer stays cached across rebuilds). These are also
# the runtime deps of ha-energy-analytics, listed here so the package itself
# can be installed --no-deps in a separate, always-refreshed layer (see
# Dockerfile) -- otherwise Docker caches a stale copy of the git package.
fastapi>=0.110
uvicorn[standard]>=0.27
pandas>=2.0
numpy>=1.24
requests>=2.28.0
