.env
__pycache__/
*.pyc
.venv/
venv/
# Runtime data volume (fingerprint JSONs + assignments.json) -- not committed,
# but keep the directory so the docker bind mount has something to mount.
data/*
!data/fingerprints
data/fingerprints/*
!data/fingerprints/.gitkeep
