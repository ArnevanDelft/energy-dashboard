.env
__pycache__/
*.pyc
.venv/
venv/
# fingerprint JSONs are synced in at runtime, not committed -- but keep the
# directory itself so the docker bind mount has something to mount.
fingerprints/*
!fingerprints/.gitkeep
